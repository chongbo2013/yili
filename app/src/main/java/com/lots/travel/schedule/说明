  日程相关的目录，里面内容为游记、行程

  schedule目录下除note、route以外都是行程与游记公共的部分。

  行程、游记在编码上几乎可以等同。但是逻辑上有一定区别。如果直接使用游记部分的写法，后期微小的需求改动将
会导致一个新的区别需要在代码上区分它们。需求变动对编码影响过大，此处使用schedule将不变化的部分
整合。

  基本思路是区分显示时的位置数据、实际的内容数据。内容数据是实际的数据，这些数据是平整结构存储的。位置结
构数据的需要将实际数据与显示的item进行对应，即需要在显示结构与实际数据结构之间进行转换。

  显示结构是按日区分的结构，而实际数据是平整结构，所以涉及到二者之间的切换。而且在编辑与预览页面之
间需要进行数据传递。显示结构对应的编码数据特点是暂时性的，且数据量在可控范围内。内容数据数据量可能会比较
大，不可控。数据量大不便于在页面之间进行传输，Bundle携带的数据量有上限。所以，显示结构需要的数据放在内存
中，内容数据在数据库与内存都有存储。从一个页面进入另一个页面时，从数据库中读取数据至内存，显示结构数据则
直接Bundle传递。

base
  edit - 编辑部分
  move - 编辑各个component顺序
  preview - 预览
  edit、move、preview对于note、route而言，是一样的。
  detail - 显示游记详情，note详情分为游记版、行程版

目录有两种分法：
  detail
    note
      mine、people
    route
      mine、people

  note
    detail
      mine、people
  route
    detail
      mine、people

  采用第二种分法



//待修改
TripPart、Spot、Hotel、Food的noteId后面改为scheduleId


